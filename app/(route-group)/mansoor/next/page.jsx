import React from "react";

const page = () => {
    return (
        <div style={{ display: "flex", height: "100vh", justifyContent: "center", alignItems: "center" }}>
            <h1>Creating multiple root layouts</h1>
        </div>
    );
};

export default page;
