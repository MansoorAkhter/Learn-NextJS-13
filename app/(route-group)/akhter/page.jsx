import React from "react";

const page = () => {
    return (
        <div style={{ display: "flex", height: "100vh", justifyContent: "center", alignItems: "center" }}>
            <h1 style={{ color: "cyan" }}>NESTED ROUTING</h1>
        </div>
    );
};

export default page;
